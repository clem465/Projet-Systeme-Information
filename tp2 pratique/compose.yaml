services:
  gameboard_db:
    image: postgres:latest
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=gameboards
    volumes:
      - ./data:/docker-entrypoint-initdb.d
    ports:
      - 5432:5432